name: CI/CD - C#

on:
  push:
    branches: [ main ]
    paths:
      - 'csharp/**'
      - '.github/workflows/ci-csharp.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'csharp/**'
  release:
    types: [ published ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v7

    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 10.0.x

    - name: Restore dependencies
      run: dotnet restore csharp/MarcusMedina.Fluent.Text.Core.slnx

    - name: Build
      run: dotnet build csharp/MarcusMedina.Fluent.Text.Core.slnx --no-restore --configuration Release

    - name: Test
      run: dotnet test csharp/MarcusMedina.Fluent.Text.Core.slnx --no-build --configuration Release --verbosity normal

  publish:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    permissions:
      id-token: write
      contents: read

    steps:
    - uses: actions/checkout@v7

    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: 10.0.x

    - name: Pack
      run: dotnet pack csharp/src/MarcusMedina.Fluent.Text.Core/MarcusMedina.Fluent.Text.Core.csproj --configuration Release

    - name: Push to NuGet (Trusted Publishing)
      working-directory: ./csharp
      run: dotnet nuget push **/*.nupkg --source https://api.nuget.org/v3/index.json --skip-duplicate
